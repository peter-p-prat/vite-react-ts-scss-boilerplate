import {common} from "./common";
import {homePage} from "./homePage";
import {user} from "./user";

export const esES = Object.seal({
    appName: "React App Boilerplate",
    common,
    user,
    homePage,
});
