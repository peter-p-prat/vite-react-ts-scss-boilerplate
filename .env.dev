# Values for development environment. DON'T PUT SECRETS HERE, use .env.development.local.
# VITE_* values are exposed to the client, so never name sensitive variables with VITE_ prefix.
VITE_BOILERPLATE_STACK=React,Vite,Vitest,TS,SCSS,RTK,router,MSW
VITE_ENABLE_DEV_TOOLS=true